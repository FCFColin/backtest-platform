# 切片06：技术债全景

> 汇总来源: 切片01-05 + 切片07 + 两份已有自检报告 (2026-07-02, 2026-07-03)
> 分析方法: 跨切片交叉验证，按 P0-P4 优先级排序

---

## 总体统计

| 优先级   | 本调研新增 | 已有自检存量 | 合计   |
| -------- | ---------- | ------------ | ------ |
| 🔴 P0    | 3          | 3            | 6      |
| 🟡 P1    | 6          | 5            | 11     |
| 🟢 P2    | 8          | 9            | 17     |
| 🔵 P3-P4 | 5          | 4            | 9      |
| **总计** | **22**     | **21**       | **43** |

---

## P0 — 必须立即修复

| #    | 债项                                                                                                                | 来源     | 影响面                             | 估算工时              | 建议执行     |
| ---- | ------------------------------------------------------------------------------------------------------------------- | -------- | ---------------------------------- | --------------------- | ------------ |
| P0-1 | **CI/CD 完全缺失** — `.github/workflows/` 目录不存在，无自动化 lint/typecheck/test 门控，无自动构建/部署            | 切片07   | 工程质量基线缺失                   | 2-3d                  | 当前 sprint  |
| P0-2 | **K8s vs Docker Compose 严重配置漂移** — Redis 在 K8s 中完全缺失；镜像名不统一；PgBouncer/OTel/HPA/PDB 仅存在于 K8s | 切片07   | K8s 部署功能不完整                 | 1-2d 评估 + 3-5d 修复 | 审计后规划   |
| P0-3 | **Go 引擎 HTTP 接口无正式契约** — 6 个 `/api/engine/*` 端点不在 OpenAPI 中；Node 侧手动构造 JSON 请求体无跨服务校验 | 切片03   | 接口变更时两侧不同步，无编译期保护 | 2-3d                  | 下一 sprint  |
| P0-4 | **测试覆盖率 3.5%**（目标 70%）— 后端服务、路由、中间件、引擎覆盖率为 ~0%                                           | 已有自检 | 回归风险高                         | 持续（40+ 人天）      | 长期工程计划 |
| P0-5 | **TypeScript 构建断裂** — `BacktestOptimizerPage.tsx` 导入不存在的符号                                              | 已有自检 | 构建/CI 无法通过                   | 0.5d                  | 本周         |
| P0-6 | **ESLint 2322 错误**（含 2289 条 no-useless-escape）                                                                | 已有自检 | CI 门控无法启用                    | 2-3d（批量 auto-fix） | 本周         |

---

## P1 — 高优先级

| #     | 债项                                                                                                                                       | 来源     | 影响面                                | 估算工时 |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------ | -------- | ------------------------------------- | -------- |
| P1-1  | **`dataManageRoutes.ts` 19 个 handler 无 zod 校验** — 数据管理突变端点完全裸露                                                             | 切片01   | 安全债 — 数据更新/导入/删除无入参校验 | 4-6h     |
| P1-2  | **告警规则不完整** — 仅有 SLO burn-rate 告警；熔断器状态、降级响应率、连接池饱和度、事件循环延迟均无告警                                   | 切片07   | 故障响应延迟                          | 1-2d     |
| P1-3  | **Redis 无持久化** — `--save '' --appendonly no`，重启丢失所有会话/限流/幂等数据                                                           | 切片07   | 数据丢失 + 限流绕过                   | 0.5d     |
| P1-4  | **PG 单副本 + 无备份机制** — StatefulSet replicas:1，无 pg_dump cronjob 或 WAL 归档                                                        | 切片07   | 数据丢失风险                          | 1-2d     |
| P1-5  | **`.dependency-cruiser.config.mjs` 路径失效** — 规则使用旧 `api/` 前缀，不匹配 `packages/backend/src/` 结构                                | 切片03   | 依赖方向自动化检查失效                | 0.5d     |
| P1-6  | **Domain event handlers 直接引用 `utils/logger`** — `RebalanceTriggeredHandler.ts`、`BacktestCompletedHandler.ts` 绕过 `DomainLogger` 接口 | 切片03   | DDD 分层违规                          | 0.5d     |
| P1-7  | **集成测试损坏** — 2/4 集成测试因路径 `api/→packages/backend/src/` 未更新而失败                                                            | 已有自检 | CI 测试门控不可用                     | 0.5d     |
| P1-8  | **K8s ConfigMap 明文 token** — `ENGINE_AUTH_TOKEN`、`DATA_SERVICE_AUTH_TOKEN` 在 ConfigMap 中而非 Secret                                   | 已有自检 | 安全债                                | 0.5d     |
| P1-9  | **`.env` 明文 API 密钥** — FINNHUB、TWELVEDATA API key 明文存储                                                                            | 已有自检 | 安全债（gitignored 但仍是风险）       | 1h       |
| P1-10 | **优化器网格搜索走 Node 引擎** — `backtestOptimizerRoutes.ts` 通过 Node 计算，与 `backtestRoutes.ts` 的 Go 优化路径不一致                  | 切片01   | 正确性债 — 两条优化路径可能结果不同   | 3-5h     |
| P1-11 | **`fetchHistoryData` 降级信息丢失** — backtest 路由内部调用时 degraded 标记被吞掉，用户看到"标的代码无效"而非"数据降级"                    | 切片04   | 用户体验降级                          | 1-2d     |

---

## P2 — 中等优先级

| #     | 债项                                                                                                   | 来源     | 影响面                   | 估算工时          |
| ----- | ------------------------------------------------------------------------------------------------------ | -------- | ------------------------ | ----------------- |
| P2-1  | **64% 前端页面残留在根目录** — 22 页面仅 8 个归入子目录，数据流模式不一致                              | 切片02   | 可维护性                 | 1-2d              |
| P2-2  | **`backtestRoutes.ts` 复合 5 种计算类型**（503行）— 应拆分为独立路由文件                               | 切片02   | 可维护性，单文件职责过重 | 2-3d              |
| P2-3  | **3 个 CRUD 路由完全同构**（portfolio/configs/runs）— 提示缺乏 CRUD 基础设施抽象                       | 切片02   | 代码重复                 | 1-2d              |
| P2-4  | **6 个前端页面直接调用 API 绕过 store** — admin 页面 + AnalysisPage + BillingPage                      | 切片03   | 状态管理不一致           | 1d                |
| P2-5  | **验证失败时旧回测结果未清除** — `backtestStore.ts` 的 `isLoading=false` 但未 `set({ results: null })` | 切片04   | 前端状态残留 bug         | 1h                |
| P2-6  | **Go 引擎 NaN/Infinity 序列化风险** — `json.Marshal` 遇到 NaN 返回 500，无法区分计算错误 vs 序列化错误 | 切片04   | 正确性债                 | 1-2d              |
| P2-7  | **新增策略类型需 8-9 个文件** — 超过目标 <5 个，Go+Node 双轨 + 前端三层 glue code 导致                 | 切片05   | 演进阻力                 | 系统设计（3-5d）  |
| P2-8  | **K8s manifests 基础镜像无 digest pin** — postgres、pgbouncer、otel-collector 使用可变 tag             | 切片07   | 安全/可复现性            | 1d                |
| P2-9  | **engine-go 无 HPA** — 仅 API 服务有 HPA，引擎无法自动扩缩容                                           | 切片07   | 性能/可靠性              | 0.5d              |
| P2-10 | **AGENTS.md 过期** — 引用旧 `api/` 和 `src/` 路径而非 `packages/`                                      | 已有自检 | 文档失准                 | 1h                |
| P2-11 | **ARCHITECTURE.md 过期** — 仍引用废弃的 Rust→Node 降级链路                                             | 已有自检 | 文档失准                 | 2h                |
| P2-12 | **runbook.md 过期** — 缺少 Go 引擎部署指南，仍引用 Rust 故障排除                                       | 已有自检 | 文档失准                 | 2h                |
| P2-13 | **Knip 死代码** — 15 未使用文件、80 未使用导出、87 未使用类型                                          | 已有自检 | 代码整洁度               | 1-2d              |
| P2-14 | **Prettier 195 文件格式不一致**                                                                        | 已有自检 | 代码风格                 | 1d（批量 format） |
| P2-15 | **Go 版本不一致** — engine-go 1.26.0 vs data-fetcher 1.26.4                                            | 已有自检 | 构建一致性               | 0.5d              |
| P2-16 | **Docker 浮动标签** — 2 个 Docker 构建阶段未固定 digest                                                | 已有自检 | 可复现性                 | 0.5d              |
| P2-17 | **`callEngineStrict<T>` 使用类型断言无运行时校验** — Go→Node 响应仅靠 `result as T`                    | 切片04   | 隐式类型不匹配风险       | 2-3d              |

---

## P3-P4 — 低优先级

| #     | 债项                                                                           | 来源     | 估算工时 |
| ----- | ------------------------------------------------------------------------------ | -------- | -------- |
| P3-1  | **`api/` 遗留骨架代码** — `api/engine/`、`api/routes/` 旧模块待清理            | 切片01   | 1-2h     |
| P3-2  | **5 个低风险裸路由补 zod 校验** — health/debug/register/admin/job routes       | 切片01   | 2h       |
| P3-3  | **`createEmptyStatistics()` 从 shared 沉到 backend**                           | 切片02   | 1h       |
| P3-4  | **`CHART_COLORS` 通过 types barrel 导出**（应走 constants 路径）               | 切片02   | 0.5h     |
| P3-5  | **路由层直接访问 `db/`** — 3 处绕过服务层                                      | 切片03   | 0.5d     |
| P3-6  | **React 19 升级** — 无硬阻断项，推荐 Q3 评估                                   | 切片01   | -        |
| P3-7  | **残留 `package-lock.json`** — pnpm 迁移遗留                                   | 切片01   | 5min     |
| P3-8  | **Repoort Outbox 重复处理风险** — at-least-once 设计决策，无 exactly-once 保证 | 切片04   | 已接受   |
| P3-9  | **`auditLog.ts` 独立写 outbox 无 event_id 去重**                               | 切片04   | 低       |
| P3-10 | **engine-go builder Dockerfile placeholder digest** — `sha256:000...`          | 切片07   | 0.5h     |
| P3-11 | **LRU 缓存实现为 FIFO**（已有自检发现）                                        | 已有自检 | -        |
| P3-12 | **`searchTickers` 加载全部后过滤**（已有自检发现）                             | 已有自检 | -        |
| P3-13 | **Redis 镜像未固定 digest**（已有自检发现）                                    | 已有自检 | 0.5h     |
| P3-14 | **DDD 3 处 LOW 违规** — 已知 pragmatism，建议当第 4 处出现时强制执行端口抽象   | 切片02   | 监控     |

---

## 关键趋势

### 相比已有自检（07-02）

- ✅ **大文件拆分已取得实质进展**：MonteCarlo 1718→85，Analysis 1617→150，jwtAuth 1093→39，app.ts 810→9，portfolio.ts 1176→379
- ✅ **包拆分完成**：从扁平 `api/`/`src/` 到 pnpm workspace（3 包）
- ⚠️ **CICD 和部署设施被严重低估**：已有自检未深入审查 CI/CD 和 K8s 配置，本次发现 P0 级缺失
- ⚠️ **ESLint 噪音 / TS 构建断裂是重构副作用**：大文件+包拆分过程中产生了新问题，P0 可快速修复

### 本调研新增关键发现

1. **CI/CD + 部署是最大盲区** — 3 个 P0 中有 2 个来自切片07（CI/CD 缺失 + K8s 配置漂移）
2. **跨服务契约缺失** — Go 引擎 vs Node API 之间的接口无正式定义，是架构层面最脆弱的耦合点
3. **已有自检的覆盖率问题被确认但优先级应下调** — 3.5% 覆盖率是长期问题，不应与 CI/CD 缺失争夺 P0 资源
