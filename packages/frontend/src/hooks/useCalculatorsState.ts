/**
 * Calculators manage their own state via individual useState calls
 * inside each calculator component. No shared state hook is needed.
 */
export function useCalculatorsState() {
  return {};
}
