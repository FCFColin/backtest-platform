-- =============================================================================
-- 回滚迁移 v28：no-op 占位，无副作用可回滚
-- 描述：v28 为 SELECT 1 no-op，回滚同样无操作。
-- =============================================================================

SELECT 1;
