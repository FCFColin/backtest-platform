# =============================================================================
# P2-08: Kustomize base — 所有 K8s 资源的基础定义
# =============================================================================
# 用法：
#   kubectl apply -k k8s/                    # 直接应用 base
#   kubectl apply -k k8s/overlays/dev         # dev 环境
#   kubectl apply -k k8s/overlays/staging     # staging 环境
#   kubectl apply -k k8s/overlays/production  # 生产环境
# ==============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: backtest-platform-base
resources:
  - namespace.yaml
  - configmap.yaml
  - auth-secret.yaml
  - api-deployment.yaml
  - api-service.yaml
  - api-hpa.yaml
  - api-pdb.yaml
  - frontend-deployment.yaml
  - frontend-service.yaml
  - frontend-pdb.yaml
  - ingress.yaml
  - engine-go-deployment.yaml
  - engine-go-service.yaml
  - engine-go-hpa.yaml
  - engine-go-pdb.yaml
  - data-fetcher-deployment.yaml
  - data-fetcher-service.yaml
  - data-fetcher-pdb.yaml
  - data-fetcher-hpa.yaml
  - worker-deployment.yaml
  - worker-pdb.yaml
  - worker-hpa.yaml
  - postgres.yaml
  - pgbouncer.yaml
  - redis.yaml
  # DEPRECATED (ADR-044): self-hosted OTel Collector 已由 SaaS 直出替代
  # 保留文件供参考，但不包含在 kustomize 构建中
  # - otel-collector.yaml
  - canary-deployment.yaml
  - mtls-issuer.yaml
  - unleash-deployment.yaml
  - prometheus-rules.yaml
  - alertmanager-deployment.yaml
  - network-policies/
